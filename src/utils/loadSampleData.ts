// Legacy utility - no longer needed since data is now served from MongoDB backend.
// Kept for reference only. Use the backend seed script instead: cd erp-backend && npm run seed

export const loadSampleData = () => {
  console.log('loadSampleData is deprecated. Use backend seed script: cd erp-backend && npm run seed');
};

export const initializeData = () => {
  console.log('initializeData is deprecated. Data is now managed by the backend API.');
};